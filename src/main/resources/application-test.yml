spring:
  cloud:
    gcp:
      pubsub:
        enabled: false
        project-id: test-project-id
        credentials:
          location: classpath:dummy-gcp-credentials.json
        topic: credit-transactions
  server:
    port: 8080
